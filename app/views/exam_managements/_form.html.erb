<%= form_for(@exam_management) do |f| %>
  <% if @exam_management.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@exam_management.errors.count, "error") %> prohibited this exam_management from being saved:</h2>

      <ul>
      <% @exam_management.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
